<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.randy.backend.dao.mapper.PersonMapper" >
    <resultMap id="BaseResultMap" type="com.randy.backend.model.Person" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="age" property="age" jdbcType="INTEGER" />
    </resultMap>

    <sql id="Base_Column_List" >
        id, name, age
    </sql>

    <select id="getAll" resultMap="BaseResultMap"  >
        SELECT
        <include refid="Base_Column_List" />
        FROM person
    </select>

    <select id="getOne" parameterType="java.lang.String" resultMap="BaseResultMap" >
        SELECT
        <include refid="Base_Column_List" />
        FROM person
        WHERE name = #{name}
    </select>

    <!--<insert id="insert" parameterType="com.randy.backend.model.Person" >-->
        <!--INSERT INTO-->
        <!--person-->
        <!--(name,age)-->
        <!--VALUES-->
        <!--(#{name}, #{age})-->
    <!--</insert>-->

    <!--<update id="update" parameterType="com.randy.backend.model.Person" >-->
        <!--UPDATE-->
        <!--person-->
        <!--SET-->
        <!--<if test="name != null">name = #{name},</if>-->
        <!--<if test="age != null">age = #{age}</if>-->
        <!--WHERE-->
        <!--id = #{id}-->
    <!--</update>-->

    <!--<delete id="delete" parameterType="java.lang.String" >-->
        <!--DELETE FROM-->
        <!--person-->
        <!--WHERE-->
        <!--name =#{name}-->
    <!--</delete>-->

</mapper>